extends: default

rules:
  line-length:
    max: 160
    level: warning
  comments:
    min-spaces-from-content: 1
  truthy:
    allowed-values: ["true", "false", "on"]
    check-keys: false
  braces:
    min-spaces-inside: 0
    max-spaces-inside: 1

