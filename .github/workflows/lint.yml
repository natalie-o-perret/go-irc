name: ci - lint

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]
    types: [opened, edited, synchronize, reopened]

jobs:
  lint:
    uses: natalie-o-perret/.github/.github/workflows/reusable-lint.yml@main
    permissions:
      pull-requests: read
