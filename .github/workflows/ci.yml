name: ci - build + tests

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  build-test:
    uses: natalie-o-perret/.github/.github/workflows/reusable-build-test-go.yml@main
    with:
      go-versions: '["1.24","1.26"]'
      go-version-latest: "1.26"
      coverage-threshold: 80
